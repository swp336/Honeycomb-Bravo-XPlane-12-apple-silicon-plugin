//
//  ToggleSet.hpp
//  Honeycomb
//
//  Created by Steve Proctor on 20/04/2023.
//

#ifndef ToggleSet_hpp
#define ToggleSet_hpp

#include <stdio.h>
#include "Toggle.hpp"
#include "XPCommand.hpp"
#include <map>
#include <unordered_map>

/*
class ToggleSet {

public:
    ToggleSet();
    ~ToggleSet();
    void addToggle(Toggle* newToggle);
    Toggle* getToggle(toggleLabel toggleToGet);
    bool isActive();
    Toggle* activeToggle();
    
private:
    std::map<toggleLabel, Toggle*> toggleSet;
    std::map<toggleLabel, Toggle*>::iterator it;
};


 
class CommandToggleSet {

public:
    CommandToggleSet();
    ~CommandToggleSet();
    void addToggle(CommandToggle* newToggle);
    CommandToggle* getToggle(toggleLabel toggleToGet);
    bool isActive();
    CommandToggle* activeToggle();
    
private:
    std::map<toggleLabel, CommandToggle*> toggleSet;
    std::map<toggleLabel, CommandToggle*>::iterator it;
};
*/

#endif /* ToggleSet_hpp */
